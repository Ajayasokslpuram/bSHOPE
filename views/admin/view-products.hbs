 <!-- Start Banner Area -->
    <section class="banner-area organic-breadcrumb">
        <div class="container">
            <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
                <div class="col-first">
                    <h1>Products</h1>
                    <nav class="d-flex align-items-center">
                        <a href="index.html">sample<span class="lnr lnr-arrow-right"></span></a>
                        <a href="category.html">View Products</a>
                    </nav>
                </div>
            </div>
        </div>
    <!-- End Banner Area -->




 <div class="checkout_btn_inner d-flex align-items-center">
                                      
                                        <a class="primary-btn" href="/admin/addproducts">Add Products</a>
</div>

{{!-- <table class="table align-middle mb-0 bg-white">
  <thead class="bg-light">
    <tr>
      <th>Title</th>
      <th>Price</th>
      <th>Stock</th>
      <th>Quantity</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    {{#each products}}
    <tr>
      <td>
        <div class="d-flex align-items-center">
          <img
              src="https://mdbootstrap.com/img/new/avatars/8.jpg"
              alt=""
              style="width: 45px; height: 45px"
              class="rounded-circle"
              />
          <div class="ms-3">
            <p class="fw-bold mb-1">{{this.name}}</p>
            <p class="text-muted mb-0">{{this.description}}</p>
          </div>
        </div>
      </td>
      <td>
        <p class="fw-normal mb-1">{{this.price}}</p>
        <p class="text-muted mb-0">{{this.price}}</p>
      </td>
      <td>
        <span class="badge badge-success rounded-pill d-inline">Active</span>
      </td>
      <td>{{this.quantity}}</td>
      <td>
        <button type="button" class="btn btn-link btn-sm btn-rounded">
          Edit
        </button>
        <button type="button" class="btn btn-link btn-sm btn-rounded">
          DELETE
        </button>
      </td>
    </tr>
  </tbody>
</table> --}}